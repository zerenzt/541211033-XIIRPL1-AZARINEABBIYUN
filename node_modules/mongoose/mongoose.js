// Assuming your 'lib/support/node-mongodb-native/lib' is in the same directory as your script
const path = require('path');
const mongoose = require(path.join(__dirname, 'lib', 'core')).Mongoose;

// Now you can use the Mongoose object as needed
